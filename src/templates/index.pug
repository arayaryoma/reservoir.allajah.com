- const __ = htmlWebpackPlugin.options.i18n
- const posts = htmlWebpackPlugin.options.posts
html
  head
    meta(charset="utf-8")
    title New Web Site
  body
    .container
      each post in posts
        a(href=`/posts/${post.filename}.html`) !{post.title}
        p !{post.date}
